generator client {
  provider = "prisma-client"
}

datasource db {
  provider = "postgresql"
}

model TestPing {
  id        String   @id @default(uuid()) @db.Uuid
  createdAt DateTime @default(now())
}
